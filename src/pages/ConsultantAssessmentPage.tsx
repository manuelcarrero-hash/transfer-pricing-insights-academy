import { FormEvent,useMemo,useState } from 'react';import { Link,Navigate } from 'react-router-dom';import { CONSULTANT_DOMAIN_FLOOR,CONSULTANT_PASS_SCORE,ConsultantDomain,ConsultantQuestion,consultantAttemptPlan,consultantBank } from '../content/assessments/consultantCumulative';
const RESULT_KEY='tp-consultant-cumulative-result';const OBJECTIVE_PASS_KEY='tp-consultant-cumulative-objective-passed';const labels:Record<ConsultantDomain,string>={C1:'Delimitación',C2:'Método',C3:'TNMM / PLI',C4:'Comparabilidad',C5:'Ajustes',C6:'Rango',C7:'Documentación'};function shuffle<T>(items:T[]){const a=[...items];for(let i=a.length-1;i>0;i--){const j=Math.floor(Math.random()*(i+1));[a[i],a[j]]=[a[j],a[i]]}return a}function makeAttempt(){return (Object.keys(consultantAttemptPlan) as ConsultantDomain[]).flatMap(domain=>shuffle(consultantBank.filter(q=>q.domain===domain)).slice(0,consultantAttemptPlan[domain]));}
export function ConsultantAssessmentPage(){const eligible=typeof window!=='undefined'&&localStorage.getItem('tp-consultant-foundations-complete')==='true';const[questions,setQuestions]=useState<ConsultantQuestion[]>(()=>shuffle(makeAttempt()));const[answers,setAnswers]=useState<Record<number,number>>({});const[result,setResult]=useState<{score:number;passedGlobal:boolean;passedDomains:boolean;domains:Record<ConsultantDomain,number>}|null>(null);const answered=Object.keys(answers).length;const domainCounts=useMemo(()=>Object.fromEntries((Object.keys(labels) as ConsultantDomain[]).map(d=>[d,questions.filter(q=>q.domain===d).length])) as Record<ConsultantDomain,number>,[questions]);if(!eligible)return <Navigate to="/path" replace/>;function grade(e:FormEvent){e.preventDefault();if(answered!==questions.length)return;const correct=questions.filter(q=>answers[q.id]===q.correctIndex).length;const score=Math.round(correct/questions.length*100);const domains={} as Record<ConsultantDomain,number>;for(const d of Object.keys(labels) as ConsultantDomain[]){const qs=questions.filter(q=>q.domain===d);const c=qs.filter(q=>answers[q.id]===q.correctIndex).length;domains[d]=Math.round(c/qs.length*100)}const passedGlobal=score>=CONSULTANT_PASS_SCORE;const passedDomains=Object.values(domains).every(x=>x>=CONSULTANT_DOMAIN_FLOOR);setResult({score,passedGlobal,passedDomains,domains});localStorage.setItem(RESULT_KEY,JSON.stringify({score,domains,passedGlobal,passedDomains,completedAt:new Date().toISOString()}));if(passedGlobal&&passedDomains)localStorage.setItem(OBJECTIVE_PASS_KEY,'true');window.scrollTo({top:0,behavior:'smooth'})}function retry(){setQuestions(shuffle(makeAttempt()));setAnswers({});setResult(null);window.scrollTo({top:0,behavior:'smooth'})}if(result){const passed=result.passedGlobal&&result.passedDomains;return <section className="section"><div className="container narrow assessment-result"><div className="eyebrow">Transfer Pricing Practitioner</div><h1>{passed?'Componente objetivo aprobado':'Necesitas reforzar uno o más dominios'}</h1><div className="result-score"><strong>{result.score}%</strong><span>calificación global</span></div><div className="materials-grid">{(Object.keys(labels) as ConsultantDomain[]).map(d=><article className="material-card" key={d}><span className="material-type">{d}</span><h3>{labels[d]}</h3><p><strong>{result.domains[d]}%</strong> · {domainCounts[d]} reactivos</p><small>{result.domains[d]>=CONSULTANT_DOMAIN_FLOOR?'Dominio acreditado':`Repasa ${d}: requiere ≥${CONSULTANT_DOMAIN_FLOOR}%`}</small></article>)}</div>{passed?<><p>Superaste ≥80% global y ningún dominio quedó debajo de 60%. Falta completar el caso integrador obligatorio para cerrar la certificación Practitioner.</p><Link className="button primary" to="/consultant/case">Resolver caso integrador</Link></>:<><p>La evaluación exige simultáneamente ≥80% global y ≥60% en cada dominio crítico.</p><button className="button primary" type="button" onClick={retry}>Intentar de nuevo</button></>}</div></section>}
return <section className="section assessment-page"><div className="container narrow"><div className="eyebrow">Evaluación acumulativa · Consultant</div><h1>Transfer Pricing Practitioner</h1><p className="lead small">24 reactivos aleatorios con cobertura estratificada de C1–C7. Necesitas 80% global y ningún dominio crítico debajo de 60%. Intentos ilimitados.</p><div className="assessment-progress"><strong>{answered} / {questions.length}</strong><span>respondidas</span></div><form onSubmit={grade}>{questions.map((q,index)=><fieldset className="assessment-question" key={q.id}><legend><span>{index+1}</span><small>{q.domain} · {labels[q.domain]}</small>{q.prompt}</legend>{q.options.map((option,oi)=><label className="assessment-option" key={option}><input type="radio" name={`q-${q.id}`} checked={answers[q.id]===oi} onChange={()=>setAnswers(c=>({...c,[q.id]:oi}))}/><span>{option}</span></label>)}</fieldset>)}<button className="button primary assessment-submit" type="submit" disabled={answered!==questions.length}>Calificar evaluación</button></form></div></section>}
